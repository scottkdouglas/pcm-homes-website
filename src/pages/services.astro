---
import BaseLayout from '../layouts/BaseLayout.astro';
import { getCollection } from 'astro:content';

const services = (await getCollection('services')).sort((a, b) => a.data.order - b.data.order);
---

<BaseLayout title="Our Services" description="Comprehensive construction services including custom homes, cottages, ICF construction, and renovations throughout Ontario.">
  <!-- Hero Section -->
  <section class="bg-gradient-to-r from-blue-600 to-blue-800 text-white py-20">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="text-center">
        <h1 class="text-4xl md:text-5xl font-bold mb-4">
          Our Services
        </h1>
        <p class="text-xl text-blue-100">
          Comprehensive construction solutions for your home building needs
        </p>
      </div>
    </div>
  </section>

  <!-- Services Content -->
  <section class="py-20">
    <div class="max-w-5xl mx-auto px-4 sm:px-6 lg:px-8">
      {await Promise.all(services.map(async (service, index) => {
        const { Content } = await service.render();
        const isEven = index % 2 === 0;

        return (
          <div id={service.slug} class={`mb-16 scroll-mt-20 ${isEven ? 'bg-white' : 'bg-gray-50'} rounded-lg p-8 shadow-md`}>
            <h2 class="text-3xl font-bold text-gray-900 mb-4">{service.data.title}</h2>
            <div class="prose prose-lg max-w-none text-gray-700">
              <Content />
            </div>
          </div>
        );
      }))}
    </div>
  </section>

  <!-- Process Section -->
  <section class="py-20 bg-blue-50">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="text-center mb-16">
        <h2 class="text-3xl md:text-4xl font-bold text-gray-900 mb-4">
          Our Working Process
        </h2>
        <p class="text-xl text-gray-600">
          A streamlined approach to bring your vision to life
        </p>
      </div>

      <div class="grid grid-cols-1 md:grid-cols-5 gap-8">
        <div class="text-center">
          <div class="w-16 h-16 bg-blue-600 text-white rounded-full flex items-center justify-center mx-auto mb-4 text-2xl font-bold">
            1
          </div>
          <h3 class="font-semibold text-lg mb-2">Design Consultation</h3>
          <p class="text-gray-600">Work with our team to define your vision</p>
        </div>

        <div class="text-center">
          <div class="w-16 h-16 bg-blue-600 text-white rounded-full flex items-center justify-center mx-auto mb-4 text-2xl font-bold">
            2
          </div>
          <h3 class="font-semibold text-lg mb-2">Budgeting</h3>
          <p class="text-gray-600">Transparent pricing and cost planning</p>
        </div>

        <div class="text-center">
          <div class="w-16 h-16 bg-blue-600 text-white rounded-full flex items-center justify-center mx-auto mb-4 text-2xl font-bold">
            3
          </div>
          <h3 class="font-semibold text-lg mb-2">Planning & PM</h3>
          <p class="text-gray-600">Expert project management coordination</p>
        </div>

        <div class="text-center">
          <div class="w-16 h-16 bg-blue-600 text-white rounded-full flex items-center justify-center mx-auto mb-4 text-2xl font-bold">
            4
          </div>
          <h3 class="font-semibold text-lg mb-2">Build</h3>
          <p class="text-gray-600">Quality craftsmanship at every stage</p>
        </div>

        <div class="text-center">
          <div class="w-16 h-16 bg-blue-600 text-white rounded-full flex items-center justify-center mx-auto mb-4 text-2xl font-bold">
            5
          </div>
          <h3 class="font-semibold text-lg mb-2">Handover</h3>
          <p class="text-gray-600">Your completed project, ready to enjoy</p>
        </div>
      </div>
    </div>
  </section>

  <!-- Credentials Section -->
  <section class="py-20 bg-white">
    <div class="max-w-5xl mx-auto px-4 sm:px-6 lg:px-8 text-center">
      <h2 class="text-3xl font-bold text-gray-900 mb-8">
        Trusted & Qualified
      </h2>
      <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
        <div class="p-6">
          <div class="text-4xl mb-3">✓</div>
          <h3 class="font-semibold text-lg mb-2">Fully Insured</h3>
          <p class="text-gray-600">Complete coverage for your peace of mind</p>
        </div>
        <div class="p-6">
          <div class="text-4xl mb-3">⏱️</div>
          <h3 class="font-semibold text-lg mb-2">9 Years Experience</h3>
          <p class="text-gray-600">Proven track record of excellence</p>
        </div>
        <div class="p-6">
          <div class="text-4xl mb-3">🛡️</div>
          <h3 class="font-semibold text-lg mb-2">2-Year Warranty</h3>
          <p class="text-gray-600">Workmanship guarantee on all builds</p>
        </div>
      </div>
    </div>
  </section>

  <!-- CTA Section -->
  <section class="py-20 bg-gradient-to-r from-blue-600 to-blue-800 text-white">
    <div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 text-center">
      <h2 class="text-3xl md:text-4xl font-bold mb-6">
        Ready to Discuss Your Project?
      </h2>
      <p class="text-xl mb-8 text-blue-100">
        Get a free quote and timeline for your construction project
      </p>
      <div class="flex flex-col sm:flex-row gap-4 justify-center">
        <a href="/contact" class="bg-white text-blue-600 px-8 py-4 rounded-lg font-semibold hover:bg-blue-50 transition-colors">
          Request a Quote
        </a>
        <a href="tel:9057161939" class="border-2 border-white text-white px-8 py-4 rounded-lg font-semibold hover:bg-white/10 transition-colors">
          Call (905) 716-1939
        </a>
      </div>
    </div>
  </section>
</BaseLayout>
